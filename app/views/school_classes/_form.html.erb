<h1> School Class Form </h1>

<%= form_for @school_class do |s| %>

    <%= s.label :title %>
    <%= s.text_area :title %>

    <%= s.label :room_number %>
    <%= s.text_area :room_number %>

   <%= s.submit %>
<% end %>